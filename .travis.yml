language: node_js
node_js:
  - stable

install:
  - npm install sequelize
  - npm install sequelize-cli

script:
  - npm run test

services:
  - postgresql

before_script:
  - psql -c 'create database travis;' -U postgres
  
after_success: 
  - npm run coverage

