language: node_js
node_js:
  - stable
env:
  - NODE_ENV=test

script:
  - npm run test

services:
  - postgresql

before_script:
  - "psql -c 'create database travis;' -U postgres"
  
after_success: 
  - npm run coverage

