language: node_js
node_js:
  - stable
env:
  - NODE_ENV=test

script:
  - npm run test:travis

services:
  - postgresql

before_script:
  - "psql -c 'create database travis;' -U postgres"
  - sequelize db:migrate
  
after_success: 
  - npm run coverage

